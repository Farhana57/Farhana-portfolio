/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Farhana-portfolio',
  assetPrefix: '/Farhana-portfolio/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;